export interface OptionsCheckedType {
  uppercaseLetters: boolean;
  lowercaseLetters: boolean;
  numbers: boolean;
  symbols: boolean;
}
export interface charsType {
  uppercaseLetters: string;
  lowercaseLetters: string;
  numbers: string;
  symbols: string;
}
